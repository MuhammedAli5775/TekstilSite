# Yüklenen dosyalar asla script olarak çalıştırılmasın (polyglot savunması).
# Apache 2.4+. Nginx'te eşdeğer kural elle verilmelidir (TAMAMLAMA_PLANI B2).
<FilesMatch "\.(php|phtml|php[0-9]|phar|pht|cgi|pl|py|sh)$">
    Require all denied
</FilesMatch>
Options -ExecCGI -Indexes
